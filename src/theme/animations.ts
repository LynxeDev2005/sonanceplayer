import { withSpring, withTiming } from 'react-native-reanimated';

export const animations = {
  spring: {
    damping: 20,
    stiffness: 200,
    mass: 1,
  },
  timing: {
    duration: 300,
  }
};
